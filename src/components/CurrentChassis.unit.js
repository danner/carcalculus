import CurrentTransmission from './CurrentTransmission'

describe('@components/CurrentTransmission', () => {
  it('exports a valid component', () => {
    expect(CurrentTransmission).toBeAComponent()
  })
})
